{
  "name": "spread-sweat-ui",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev-docker": "vite --port 8010 --host 0.0.0.0",
    "dev": "vite",
    "build": "vue-tsc && vite build",
    "test": "vitest",
    "preview": "vite preview",
    "up": "npm run build && dotenv -e .env -- serve -s dist -l $VITE_UI_PORT"
  },
  "dependencies": {
    "dotenv-cli": "^7.3.0",
    "serve": "^14.2.1",
    "vue": "^3.3.4"
  },
  "devDependencies": {
    "@faker-js/faker": "^8.2.0",
    "@vitejs/plugin-vue": "^4.2.3",
    "typescript": "^5.0.2",
    "vite": "^4.4.5",
    "vitest": "^0.34.6",
    "vue-tsc": "^1.8.5"
  }
}
